# PC 到 ECS 的 Go2/M4T 指令接口

> 整理日期：2026-08-21
>
> 本文只描述 PC 或上层控制程序调用 ECS 的接口，不包含 ECS 到设备、Go2 Orsus 内部接口和 M4T PSDK 接口。

## 1. 连接信息

| 项目 | 当前值 |
| --- | --- |
| ECS Base URL | `http://120.24.74.70` |
| Go2 设备 ID | `ORSUS-GO2-GSM20260003` |
| M4T 设备 ID | `M4T-001` |
| 数据格式 | `application/json` |
| 认证 | Operator Bearer Token |

所有请求使用以下请求头：

```http
Authorization: Bearer <operator-token>
Content-Type: application/json
Accept: application/json
```

当前地址是临时公网 HTTP，仅用于受控联调。正式使用应改成 HTTPS。

## 2. PC 侧接口清单

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `POST` | `/v1/devices/{device_id}/commands` | 向 Go2 或 M4T 下发指令 |
| `GET` | `/v1/commands/{command_id}` | 查询指令执行状态和结果 |
| `GET` | `/v1/devices` | 查询设备列表及在线状态 |
| `GET` | `/v1/devices/{device_id}/status` | 查询指定设备的最新状态 |
| `GET` | `/v1/time` | 获取 ECS 时间，用于时延校准 |
| `GET` | `/health` | ECS 健康检查，无需 Token |

核心控制只需要前两个接口。

## 3. 下发指令

```http
POST /v1/devices/{device_id}/commands
```

通用请求体：

```json
{
  "client_request_id": "<uuid>",
  "type": "<command-type>",
  "payload": {}
}
```

字段说明：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `client_request_id` | UUID 字符串 | 是 | PC 生成的幂等请求 ID |
| `type` | 字符串 | 是 | 指令类型 |
| `payload` | JSON 对象 | 是 | 指令参数，无参数时传 `{}` |

首次创建返回 HTTP `201`。使用完全相同的 `client_request_id`、设备、指令类型和 payload 重试时返回原指令，HTTP `200`；同一 ID 对应不同指令数据时返回 `409`。

## 4. 通用指令

Go2 和 M4T 都支持以下指令。

### 4.1 链路检查 PING

```json
{
  "client_request_id": "<uuid>",
  "type": "PING",
  "payload": {}
}
```

### 4.2 设备状态查询 STATUS_QUERY

```json
{
  "client_request_id": "<uuid>",
  "type": "STATUS_QUERY",
  "payload": {}
}
```

### 4.3 控制前启动 STARTUP

```json
{
  "client_request_id": "<uuid>",
  "type": "STARTUP",
  "payload": {}
}
```

必须等待 STARTUP 指令变为 `COMPLETED`，且 `result.status` 为 `ready`，才能提交 `NAVIGATE`。

## 5. Go2 指令

### 5.1 Go2 导航

请求：

```http
POST /v1/devices/ORSUS-GO2-GSM20260003/commands
```

请求体：

```json
{
  "client_request_id": "<uuid>",
  "type": "NAVIGATE",
  "payload": {
    "target": {
      "x": -5.303,
      "y": 13.740,
      "theta": -1.5987215948268056
    }
  }
}
```

目标字段：

| 字段 | 类型 | 单位/坐标系 |
| --- | --- | --- |
| `x` | 浮点数 | 当前 Orsus 地图坐标，米 |
| `y` | 浮点数 | 当前 Orsus 地图坐标，米 |
| `theta` | 浮点数 | 朝向，弧度 |

当前 Go2 地图固定为 `airs1f_3`。PC 不应发送地图名或适配器参数。

### 5.2 取消 Go2 导航

```json
{
  "client_request_id": "<new-uuid>",
  "type": "CANCEL_NAVIGATION",
  "payload": {
    "navigation_command_id": "<go2-navigate-command-id>"
  }
}
```

`navigation_command_id` 必须是同一 Go2 设备上的 `NAVIGATE` 指令 ID。

## 6. M4T 指令

### 6.1 M4T 导航

请求：

```http
POST /v1/devices/M4T-001/commands
```

请求体：

```json
{
  "client_request_id": "<uuid>",
  "type": "NAVIGATE",
  "payload": {
    "target": {
      "latitude_deg": 22.604375789,
      "longitude_deg": 114.057071644,
      "altitude_ellipsoid_m": 106.0
    }
  }
}
```

目标字段：

| 字段 | 类型 | 单位/坐标系 |
| --- | --- | --- |
| `latitude_deg` | 浮点数 | WGS84 纬度，度，范围 `[-90, 90]` |
| `longitude_deg` | 浮点数 | WGS84 经度，度，范围 `[-180, 180]` |
| `altitude_ellipsoid_m` | 浮点数 | WGS84 椭球高，米 |

`altitude_ellipsoid_m` 不是相对起飞点高度，也不是海拔高。

M4T 的 STARTUP 是一次性凭据：成功后需在 5 分钟内提交一次 NAVIGATE；被使用、超时、ECS 重启或设备会话变化后，需要重新 STARTUP。

### 6.2 取消飞行中的 M4T 导航

```json
{
  "client_request_id": "<new-uuid>",
  "type": "CANCEL_NAVIGATION",
  "payload": {
    "navigation_command_id": "<m4t-navigate-command-id>"
  }
}
```

该指令用于活动中的 M4T 导航，设备端会取消任务并执行返航。

### 6.3 M4T 导航完成后返航

```json
{
  "client_request_id": "<uuid>",
  "type": "RETURN_HOME",
  "payload": {}
}
```

活动 NAVIGATE 期间不能提交 `RETURN_HOME`，应使用 `CANCEL_NAVIGATION`。Go2 不支持 `RETURN_HOME`。

## 7. 创建指令响应

示例：

```json
{
  "command_id": "<command-uuid>",
  "client_request_id": "<client-request-uuid>",
  "device_id": "ORSUS-GO2-GSM20260003",
  "type": "NAVIGATE",
  "payload": {
    "target": {
      "x": -5.303,
      "y": 13.74,
      "theta": -1.5987215948268056
    }
  },
  "state": "QUEUED",
  "delivery_count": 0,
  "progress": null,
  "result": null,
  "error": null
}
```

PC 应保存返回的 `command_id`，后续通过该 ID 查询执行结果。

## 8. 查询指令状态

```http
GET /v1/commands/{command_id}
```

状态流转：

```text
QUEUED -> DELIVERED -> RECEIVED -> RUNNING -> COMPLETED
                                      |----> FAILED
                                      `----> CANCELLED
```

| 状态 | PC 侧含义 |
| --- | --- |
| `QUEUED` | 指令在 ECS 排队，设备尚未领取 |
| `DELIVERED` | 指令已投递给设备 |
| `RECEIVED` | 设备确认收到 |
| `RUNNING` | 正在执行，阶段信息在 `progress` |
| `COMPLETED` | 成功结束，结果在 `result` |
| `FAILED` | 执行失败，原因在 `error` |
| `CANCELLED` | 导航已取消 |

PC 轮询建议：

- 每 `0.5s` 至 `1s` 查询一次。
- 收到 `COMPLETED`、`FAILED` 或 `CANCELLED` 后停止轮询。
- `QUEUED` 且 `delivery_count=0` 表示设备还没有领取指令。
- HTTP 超时或 `502/503/504` 时可以继续使用同一个 `client_request_id` 或 `command_id` 重试，不要生成重复控制任务。

## 9. 查询设备状态

### 9.1 设备列表

```http
GET /v1/devices
```

用于确认目标设备是否 `online=true`。

### 9.2 指定设备状态

```http
GET /v1/devices/{device_id}/status
```

建议在 STARTUP 前调用，确认设备在线且最新遥测无明显错误。

## 10. HTTP 状态码

| HTTP 状态 | 含义 |
| --- | --- |
| `200` | 查询成功，或幂等重试返回已有指令 |
| `201` | 新指令创建成功 |
| `401` | Operator Token 缺失或错误 |
| `403` | ECS 禁止通过当前 HTTP 链路下发运动指令 |
| `404` | 设备、指令或取消目标不存在 |
| `409` | 已有活动任务、STARTUP 无效或幂等 ID 冲突 |
| `422` | 指令类型、payload 或坐标字段不符合接口定义 |

## 11. curl 示例

设置变量：

```bash
export RELAY_BASE_URL='http://120.24.74.70'
export RELAY_OPERATOR_TOKEN='<operator-token>'
```

提交 Go2 STARTUP：

```bash
curl -sS -X POST \
  "$RELAY_BASE_URL/v1/devices/ORSUS-GO2-GSM20260003/commands" \
  -H "Authorization: Bearer $RELAY_OPERATOR_TOKEN" \
  -H 'Content-Type: application/json' \
  -d "{\"client_request_id\":\"$(uuidgen)\",\"type\":\"STARTUP\",\"payload\":{}}"
```

查询指令：

```bash
curl -sS \
  "$RELAY_BASE_URL/v1/commands/<command-id>" \
  -H "Authorization: Bearer $RELAY_OPERATOR_TOKEN"
```

生产程序应使用语言自带的 JSON 序列化库和 UUID 库构造请求，不要拼接 JSON 字符串。
