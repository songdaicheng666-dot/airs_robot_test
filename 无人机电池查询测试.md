# 通过查询无人机电池电量测试5G通信的方法

## 具体方法
1. 新建两个终端，终端一用于登录妙算3,终端二用于本机操作。妙算3登录IP为 `ssh dji@192.168.0.69`
2. 准备运行目录：
在登录妙算3的终端一中输入,用于临时安装目录：
```
cd /home/dji/m4t-communication-test/native-build/source

install -d -m 700 ../run

install -m 700 \
  build-m4t-relay/bin/dji_sdk_demo_on_manifold3 \
  ../run/

install -m 600 \
  Payload-SDK-master/samples/sample_c/platform/linux/manifold3/app_json/m4t_relay_config.json \
  ../run/

cd ../run
ls -l

```
预期结果:
终端中出现如下内容则表示安装成功
```
dji_sdk_demo_on_manifold3
m4t_relay_config.json

```

3. 停止冲突程序`Smart3DExplore`
妙算3中存在`Smart3DExplore`进程，此进程将会阻碍PSDK正常运行，因此需要实现关闭
```
dji_app_ctl stop Smart3DExplore
sleep 3

ps -eo pid,comm,args | \
  grep -E '[S]mart3DExplore|[d]ji_sdk_demo_on_manifold3|[w]eishi'
  
```

4. 启动前台Relay
在妙算3终端中输入以下指令后需要保持该终端。
```
cd /home/dji/m4t-communication-test/native-build/run
./dji_sdk_demo_on_manifold3 2>&1 | tee psdk-console.log

```

5. 电脑终端 B：查询云端
在终端二中输入以下指令：
```
cd /home/sdc/文档/real_robot_test

set -a
source communication_test/.private/m4t-relay.env
set +a

export M4T_BASE_URL=http://120.24.74.70

```

查询设备是否在线：
```
python3 -m communication_test.pc.client devices

```

若设备正常则显示：
```
"online": true

```

6. 查询状态
```
python3 -m communication_test.pc.client status

```
若一切正常，终端就会出现
```
"battery": {
  "valid": true,
  "percentage": 75,
  "voltage_v": 23.1,
  "current_a": -1.2
}

```

7. 测试结束恢复原样
```
ps -eo pid,comm,args | grep '[d]ji_sdk_demo_on_manifold3'

dji_app_ctl start Smart3DExplore
sleep 5

ps -eo pid,comm,args | grep '[S]mart3DExplore'

```

