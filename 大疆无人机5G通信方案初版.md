# 大疆无人机 5G 通信方案初版

## 适用范围

- 飞行器：DJI Matrice 4T
- 机载计算：妙算 3
- 网络：妙算 3 通过 5G 随身 WiFi 访问公网
- 场景：室外、运行时下发一个或一系列目标点
- 飞行方式：PSDK FlyTo，逐点执行
- 通信方式：HTTPS 长轮询
- 定位方式：GNSS/RTK，不需要预先建图

## 总体架构

```text
┌──────────────┐       HTTPS POST       ┌─────────────────────────┐
│ 端侧大模型   │ ─────────────────────► │       公网云服务器       │
│ 目标点生成   │                        │                         │
└──────────────┘                        │  ┌───────────────────┐  │
        ▲                               │  │ 身份认证/API网关   │  │
        │ 查询任务状态                  │  ├───────────────────┤  │
        └────────────────────────────── │  │ 路线与命令服务     │  │
                                        │  ├───────────────────┤  │
                                        │  │ 命令/状态数据库    │  │
                                        │  └───────────────────┘  │
                                        └───────────┬─────────────┘
                                                    │
                                      HTTPS 长轮询  │  HTTPS 状态上报
                                                    │
                                             公网/5G 网络
                                                    │
                                        ┌───────────▼─────────────┐
                                        │       5G 随身 WiFi       │
                                        └───────────┬─────────────┘
                                                    │ 局域网
┌───────────────────────────────────────────────────▼──────────────┐
│                            妙算 3                                │
│                                                                  │
│  通信服务 → 命令校验 → 本地路线队列 → 导航状态机 → PSDK适配层   │
│      ▲           │             │              │          │       │
│      └────状态上报┴────安全策略─┴────本地缓存──┘          │       │
└───────────────────────────────────────────────────────────┼───────┘
                                                            │ FlyTo
                                                  ┌─────────▼────────┐
                                                  │   Matrice 4T      │
                                                  │ 自动规划与避障    │
                                                  └──────────────────┘
```

## 通信流程

```text
1. 大模型生成目标路线
2. 大模型通过 HTTPS POST 将路线提交给云服务器
3. 云服务器校验并持久化，返回 command_id
4. 妙算 3 持续向云服务器发起 HTTPS 长轮询
5. 云服务器在长轮询响应中返回待执行路线
6. 妙算 3 返回 RECEIVED，随后进行本地安全校验
7. 校验通过后写入本地路线队列，并返回 ACCEPTED
8. 妙算 3 对第一个点调用 FlyTo
9. 到达后自动执行队列中的下一个点
10. 妙算 3 持续向云服务器 POST 进度和结果
11. 全部目标完成后按策略返航
```


## 端侧模型部署方式说明

当前阶段计划将面向端侧的 7B 量化大模型部署在开发侧本地算力设备上，通过前述通信链路向云服务器提交目标点或路线，再由妙算 3 获取并执行，以此模拟大模型直接部署在无人机端侧的完整业务流程。采用该方案主要基于以下两点考虑：

1. **妙算 3 的内存容量无法满足模型完整部署要求**

   妙算 3 采用 16 GB 内存的 Orin 平台。模型运行时的内存占用不仅包括量化后的权重，还包括 Python 与推理框架运行时、CUDA 上下文、视觉编码模块、中间激活、KV Cache 和其他推理缓冲区，同时还需为操作系统、PSDK 及导航通信程序预留资源。在不缩减上下文 Token 配置、不调整模型参数、不降低推理能力，并保持与当前验证环境完全一致的前提下，16 GB 内存不足以稳定、完整地部署该模型。

2. **妙算 3 的软件环境与当前模型环境不一致**

   当前模型已经在 Python 3.10、PyTorch 2.2 环境中完成配置和验证，而妙算 3 平台当前仅支持 Python 3.8、PyTorch 2.1。直接迁移会引入模型代码、算子、量化库、CUDA 内核及推理框架之间的兼容性问题；通过降级依赖适配妙算 3，也会改变已验证的运行环境，无法满足模型参数、上下文配置和推理行为保持一致的要求。

因此，初期方案将模型推理与无人机控制解耦：本地算力设备负责以原有配置运行模型并生成结构化目标信息，云服务器负责命令中转和状态管理，妙算 3 负责通信、安全校验、目标队列管理及 PSDK 飞行控制。该方式能够在不牺牲模型配置和能力的情况下，先完成从模型决策到无人机执行的端到端流程验证。
